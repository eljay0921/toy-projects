spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/coupon
    username: coupon
    password: coupon123

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        jdbc.time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration

  data:
    redis:
      host: redis
      port: 6379

server:
  port: 8080
